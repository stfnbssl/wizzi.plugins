module
    kind es6
    $
        var md = {
        };
    v08/lib/wizzi/models/mtree-preprocessor(&md)
        function preprocessNode
            param node
            param state
            if node.n === 'xxx'
                # TODO
            foreach child in node.children
                _ preprocessNode
                    @ child
                    @ state
