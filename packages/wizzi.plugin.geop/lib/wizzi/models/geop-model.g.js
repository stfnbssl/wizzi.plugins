/*
    artifact generator: C:\My\wizzi\stfnbssl\wizzi.plugins\packages\wizzi.plugin.wzschema\node_modules\wizzi-js\lib\artifacts\js\module\gen\main.js
    package: wizzi-js@0.7.13
    primary source IttfDocument: C:\My\wizzi\stfnbssl\wizzi.plugins\packages\wizzi.plugin.wzschema\lib\artifacts\wfschema\model\gen\ittf\wfschema-model.js.ittf
*/
'use strict';
// generated by v6-wizzi-js.artifacts.js.module.main
function _inherits(subClass, superClass) { if (typeof superClass !== 'function' && superClass !== null) { throw new TypeError('Super expression must either be null or a function, not ' + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError('Cannot call a class as a function'); } }
var _get = function get(_x, _x2, _x3) { var _again = true; _function: while (_again) { var object = _x, property = _x2, receiver = _x3; _again = false; if (object === null) object = Function.prototype; var desc = Object.getOwnPropertyDescriptor(object, property); if (desc === undefined) { var parent = Object.getPrototypeOf(object); if (parent === null) { return undefined; } else { _x = parent; _x2 = property; _x3 = receiver; _again = true; desc = parent = undefined; continue _function; } } else if ('value' in desc) { return desc.value; } else { var getter = desc.get; if (getter === undefined) { return undefined; } return getter.call(receiver); } } };

var util = require('util');
var path = require('path');
var _ = require('lodash');
var chalk = require('chalk');
var wzutils = require('wizzi-utils');
var verify = wzutils.verify;


var _md = module.exports = {};

var wzSourceLineInfo = (function () {
    function wzSourceLineInfo(row, col, sourceKey) {
        _classCallCheck(this, wzSourceLineInfo);
        this.row = row;
        this.col = col;
        this.sourceKey = sourceKey;
    }
    wzSourceLineInfo.prototype.toString = function(instance) {
        if (instance) {
            return 'row: ' + this.row + ', col: ' + this.col + ', file: ' + instance.wzSourceFilepath(this.sourceKey);
        }
        else {
            return 'row: ' + this.row + ', col: ' + this.col + ', sourceKey: ' + this.sourceKey;
        }
    }
    return wzSourceLineInfo;
})();

_md.wzSourceLineInfo = wzSourceLineInfo;
var geopBase = (function () {
    function geopBase(name, sourceLineInfo) {
        _classCallCheck(this, geopBase);
        this.wzName = name || '';
        this.wzParent = null;
        this.wzSourceLineInfo = sourceLineInfo;
        this.wzChildren = [];
    }
    geopBase.prototype.wzRoot = function() {
        return this.wzParent == null ? this : this.wzParent.wzRoot();
    }
    geopBase.prototype.wzSourceFilepath = function(sourceKey) {
        var sk = sourceKey || this.wzSourceLineInfo.sourceKey;
        return this.wzRoot().loadHistory.getIttfDocumentUri(sk);
    }
    geopBase.prototype.wzSourceErrorLines = function(node, message) {
        return this.wzRoot().loadHistory.getIttfDocumentErrorLines(node.u, {
                row: node.r, 
                col: node.c, 
                description: message
             }, true);
    }
    geopBase.prototype.wzVerify = function() {
    }
    geopBase.prototype.wzInitialize = function() {
    }
    geopBase.prototype.wzInitializeAsync = function(ctx, callback) {
        callback(null);
    }
    geopBase.prototype.wzAddChild = function(node) {
        node.wzParent = this;
        this.wzChildren.push(node);
    }
    geopBase.prototype.wzAddChildToColl = function(node, coll) {
        node.wzParent = this;
        node.wzMoved = true;
        coll.push(node);
    }
    geopBase.prototype.wzMoveChildToColl = function(node, coll, fromColl) {
        var index = fromColl.indexOf(node);
        if (index < 0) {
            this.error('wzMoveChildToColl error. The from collection does not contain the node.', node)
        }
        fromColl.splice(index, 1);
        this.wzAddChildToColl(node, coll);
    }
    geopBase.prototype.wzLoadToChildColl = function(child, type, coll) {
        var item = new type(child.v, new wzSourceLineInfo(child.r, child.c, child.u));
        item.wzTag = child.n;
        if (child.wzMTreeData) {
            item.wzMTreeData = child.wzMTreeData;
        }
        item.wzParent = this;
        coll.push(item);
        if (item.loadFromNode) {
            item.loadFromNode(child);
        }
        return true;
    }
    geopBase.prototype.wzCreateChildColl = function(tag, name, type, coll) {
        var item = new type(name, this.wzSourceLineInfo);
        item.wzTag = tag;
        item.wzParent = this;
        item.wzCreated = true;
        coll.push(item);
        return item;
    }
    geopBase.prototype.wzLoadToChildren = function(child, type) {
        var item = new type(child.v, new wzSourceLineInfo(child.r, child.c, child.u));
        item.wzTag = child.n;
        item.wzParent = this;
        if (child.wzMTreeData) {
            item.wzMTreeData = child.wzMTreeData;
        }
        this.wzChildren.push(item);
        if (item.loadFromNode) {
            item.loadFromNode(child);
        }
        return true;
    }
    geopBase.prototype.wzLoadOneToOne = function(child, type, fieldName) {
        var item = new type(child.v, new wzSourceLineInfo(child.r, child.c, child.u));
        if (child.wzMTreeData) {
            item.wzMTreeData = child.wzMTreeData;
        }
        item.wzParent = this;
        this[fieldName] = item;
        if (item.loadFromNode) {
            item.loadFromNode(child);
        }
        return true;
    }
    geopBase.prototype.wzRemove = function(fromColl) {
        var index = fromColl.indexOf(this);
        if (index < 0) {
            this.error('wzRemove error. The from collection does not contain the node.', this)
        }
        fromColl.splice(index, 1);
    }
    geopBase.prototype.error = function(message, node) {
        throw new _md.geopModelException(message, node, this);
    }
    geopBase.prototype.toJsonProperties = function(result) {
        result.wzElement = this.wzElement;
        result.wzTag = this.wzTag;
        result.wzName = this.wzName;
    }
    geopBase.prototype.toJsonChildren = function(result) {
        if (this.wzChildren.length > 0) {
            var children = [];
            this.wzChildren.forEach(item => 
            
                children.push(item.toJson())
            )
            result.wzChildren = children;
        }
    }
    return geopBase;
})();

_md.geopBase = geopBase;

// element item
var item = (function (geopBase) {
    _inherits(item, geopBase);
    function item(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(item.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, item);
        this.wzElement = "item";
        // relation item
        this.items = [];
    }
    item.prototype.addItem = function(name, sourceLineInfo) {
        var retval = new _md.item(name, sourceLineInfo);
        retval.wzParent = this;
        this.items.push(retval);
        return retval;
    }
    item.prototype.getItem = function(name) {
        var found = null;
        this.items.forEach(function(item) {
            found = found || (item.wzName === name ? item : null);
        })
        return found;
    }
    item.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    item.prototype.toJsonProperties = function(result) {
        _md.geopBase.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
        if (!isEmpty(this.value)) {
            result["value"] = this.value;
        }
    }
    item.prototype.toJsonChildren = function(result) {
        _md.geopBase.prototype.toJsonChildren.call(this, result);
        var items, item;
        items = [];
        this.items.forEach(item => 
        
            items.push(item.toJson())
        )
        result['items'] = items;
    }
    item.prototype.loadChild = function(child) {
        var name = child.n.toLowerCase();
        if (name === '+') {
            return this.wzLoadToChildColl(child, _md.text, this.items);
        }
        if (name === 'a') {
            return this.wzLoadToChildColl(child, _md.a, this.items);
        }
        if (name === 'en') {
            return this.wzLoadToChildColl(child, _md.en, this.items);
        }
        if (name === 'it') {
            return this.wzLoadToChildColl(child, _md.it, this.items);
        }
        if (name === 'li') {
            return this.wzLoadToChildColl(child, _md.li, this.items);
        }
        if (name === 'to') {
            return this.wzLoadToChildColl(child, _md.to, this.items);
        }
        if (name === 'act') {
            return this.wzLoadToChildColl(child, _md.act, this.items);
        }
        if (name === 'end') {
            return this.wzLoadToChildColl(child, _md.end, this.items);
        }
        if (name === 'img') {
            return this.wzLoadToChildColl(child, _md.img, this.items);
        }
        if (name === 'ref') {
            return this.wzLoadToChildColl(child, _md.ref, this.items);
        }
        if (name === 'url') {
            return this.wzLoadToChildColl(child, _md.url, this.items);
        }
        if (name === 'war') {
            return this.wzLoadToChildColl(child, _md.war, this.items);
        }
        if (name === 'born') {
            return this.wzLoadToChildColl(child, _md.born, this.items);
        }
        if (name === 'date') {
            return this.wzLoadToChildColl(child, _md.date, this.items);
        }
        else if (name === 'died') {
            return this.wzLoadToChildColl(child, _md.died, this.items);
        }
        if (name === 'from') {
            return this.wzLoadToChildColl(child, _md.from, this.items);
        }
        if (name === 'geop') {
            return this.wzLoadToChildColl(child, _md.geop, this.items);
        }
        if (name === 'item') {
            return this.wzLoadToChildColl(child, _md.item, this.items);
        }
        if (name === 'king') {
            return this.wzLoadToChildColl(child, _md.king, this.items);
        }
        else if (name === 'king') {
            return this.wzLoadToChildColl(child, _md.king, this.items);
        }
        if (name === 'wiki') {
            return this.wzLoadToChildColl(child, _md.wiki, this.items);
        }
        if (name === 'year') {
            return this.wzLoadToChildColl(child, _md.year, this.items);
        }
        if (name === 'alias') {
            return this.wzLoadToChildColl(child, _md.alias, this.items);
        }
        else if (name === 'agent') {
            return this.wzLoadToChildColl(child, _md.agent, this.items);
        }
        if (name === 'child') {
            return this.wzLoadToChildColl(child, _md.child, this.items);
        }
        if (name === 'event') {
            return this.wzLoadToChildColl(child, _md.event, this.items);
        }
        if (name === 'house') {
            return this.wzLoadToChildColl(child, _md.house, this.items);
        }
        if (name === 'place') {
            return this.wzLoadToChildColl(child, _md.place, this.items);
        }
        else if (name === 'place') {
            return this.wzLoadToChildColl(child, _md.place, this.items);
        }
        else if (name === 'party') {
            return this.wzLoadToChildColl(child, _md.party, this.items);
        }
        if (name === 'quote') {
            return this.wzLoadToChildColl(child, _md.quote, this.items);
        }
        if (name === 'start') {
            return this.wzLoadToChildColl(child, _md.start, this.items);
        }
        else if (name === 'state') {
            return this.wzLoadToChildColl(child, _md.state, this.items);
        }
        else if (name === 'state') {
            return this.wzLoadToChildColl(child, _md.state, this.items);
        }
        if (name === 'title') {
            return this.wzLoadToChildColl(child, _md.title, this.items);
        }
        if (name === 'value') {
            this.value = child.v; return true;
        }
        if (name === 'author') {
            return this.wzLoadToChildColl(child, _md.author, this.items);
        }
        else if (name === 'avatar') {
            return this.wzLoadToChildColl(child, _md.avatar, this.items);
        }
        if (name === 'period') {
            return this.wzLoadToChildColl(child, _md.period, this.items);
        }
        else if (name === 'parent') {
            return this.wzLoadToChildColl(child, _md.parent, this.items);
        }
        else if (name === 'person') {
            return this.wzLoadToChildColl(child, _md.person, this.items);
        }
        if (name === 'regent') {
            return this.wzLoadToChildColl(child, _md.regent, this.items);
        }
        if (name === 'source') {
            return this.wzLoadToChildColl(child, _md.source, this.items);
        }
        else if (name === 'status') {
            return this.wzLoadToChildColl(child, _md.status, this.items);
        }
        if (name === 'treaty') {
            return this.wzLoadToChildColl(child, _md.treaty, this.items);
        }
        if (name === 'context') {
            return this.wzLoadToChildColl(child, _md.context, this.items);
        }
        if (name === 'enemies') {
            return this.wzLoadToChildColl(child, _md.enemies, this.items);
        }
        if (name === 'founder') {
            return this.wzLoadToChildColl(child, _md.founder, this.items);
        }
        else if (name === 'friends') {
            return this.wzLoadToChildColl(child, _md.friends, this.items);
        }
        if (name === 'synonim') {
            return this.wzLoadToChildColl(child, _md.synonim, this.items);
        }
        if (name === 'alliance') {
            return this.wzLoadToChildColl(child, _md.alliance, this.items);
        }
        if (name === 'coauthor') {
            return this.wzLoadToChildColl(child, _md.coauthor, this.items);
        }
        if (name === 'fullname') {
            return this.wzLoadToChildColl(child, _md.fullname, this.items);
        }
        if (name === 'incharge') {
            return this.wzLoadToChildColl(child, _md.inCharge, this.items);
        }
        if (name === 'minister') {
            return this.wzLoadToChildColl(child, _md.minister, this.items);
        }
        else if (name === 'mediator') {
            return this.wzLoadToChildColl(child, _md.mediator, this.items);
        }
        else if (name === 'minister') {
            return this.wzLoadToChildColl(child, _md.minister, this.items);
        }
        if (name === 'repealed') {
            return this.wzLoadToChildColl(child, _md.repealed, this.items);
        }
        if (name === 'co-author') {
            return this.wzLoadToChildColl(child, _md.coauthor, this.items);
        }
        else if (name === 'criticism') {
            return this.wzLoadToChildColl(child, _md.criticism, this.items);
        }
        if (name === 'exchequer') {
            return this.wzLoadToChildColl(child, _md.exchequer, this.items);
        }
        if (name === 'in-charge') {
            return this.wzLoadToChildColl(child, _md.inCharge, this.items);
        }
        if (name === 'jussystem') {
            return this.wzLoadToChildColl(child, _md.jusSystem, this.items);
        }
        if (name === 'namespace') {
            return this.wzLoadToChildColl(child, _md.namespace, this.items);
        }
        if (name === 'opinionof') {
            return this.wzLoadToChildColl(child, _md.opinionof, this.items);
        }
        if (name === 'pretender') {
            return this.wzLoadToChildColl(child, _md.pretender, this.items);
        }
        if (name === 'signature') {
            return this.wzLoadToChildColl(child, _md.signature, this.items);
        }
        if (name === 'curriculum') {
            return this.wzLoadToChildColl(child, _md.curriculum, this.items);
        }
        if (name === 'influenced') {
            return this.wzLoadToChildColl(child, _md.influenced, this.items);
        }
        if (name === 'jus-system') {
            return this.wzLoadToChildColl(child, _md.jusSystem, this.items);
        }
        else if (name === 'jusacttype') {
            return this.wzLoadToChildColl(child, _md.jusActType, this.items);
        }
        if (name === 'opinion-of') {
            return this.wzLoadToChildColl(child, _md.opinionof, this.items);
        }
        if (name === 'description') {
            return this.wzLoadToChildColl(child, _md.description, this.items);
        }
        if (name === 'authorsgroup') {
            return this.wzLoadToChildColl(child, _md.authorsgroup, this.items);
        }
        else if (name === 'attributedby') {
            return this.wzLoadToChildColl(child, _md.attributedby, this.items);
        }
        if (name === 'influencedby') {
            return this.wzLoadToChildColl(child, _md.influencedby, this.items);
        }
        if (name === 'jus-act-type') {
            return this.wzLoadToChildColl(child, _md.jusActType, this.items);
        }
        if (name === 'authors-group') {
            return this.wzLoadToChildColl(child, _md.authorsgroup, this.items);
        }
        else if (name === 'attributed-by') {
            return this.wzLoadToChildColl(child, _md.attributedby, this.items);
        }
        if (name === 'homesecretary') {
            return this.wzLoadToChildColl(child, _md.homeSecretary, this.items);
        }
        if (name === 'influenced-by') {
            return this.wzLoadToChildColl(child, _md.influencedby, this.items);
        }
        if (name === 'primeminister') {
            return this.wzLoadToChildColl(child, _md.primeMinister, this.items);
        }
        if (name === 'home-secretary') {
            return this.wzLoadToChildColl(child, _md.homeSecretary, this.items);
        }
        if (name === 'prime-minister') {
            return this.wzLoadToChildColl(child, _md.primeMinister, this.items);
        }
        if (name === 'foreignsecretary') {
            return this.wzLoadToChildColl(child, _md.foreignSecretary, this.items);
        }
        if (name === 'foreign-secretary') {
            return this.wzLoadToChildColl(child, _md.foreignSecretary, this.items);
        }
        return false;
    }
    item.prototype.loadFromNode = function(node) {
        node.children.forEach((item) => {
        
            var loaded = this.loadChild(item);
            if (!loaded) {
                throw new _md.geopModelException("Tag not recognized: " + item.n, item, this);
            }
        }
        )
    }
    item.prototype.wzVerify = function(ctx) {
        this.items.forEach(item => 
        
            item.wzVerify(ctx)
        )
        _md.geopBase.prototype.wzVerify.call(this, ctx);
    }
    item.prototype.wzInitialize = function(ctx) {
        this.items.forEach(item => 
        
            item.wzInitialize(ctx)
        )
        _md.geopBase.prototype.wzInitialize.call(this, ctx);
    }
    return item;
})(geopBase);

_md.item = item;
// element geop
var geop = (function (item) {
    _inherits(geop, item);
    function geop(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(geop.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, geop);
        this.wzElement = "geop";
        // relation ittfPanel
        this.ittfPanels = [];
    }
    geop.prototype.addIttfPanel = function(name, sourceLineInfo) {
        var retval = new _md.ittfPanel(name, sourceLineInfo);
        retval.wzParent = this;
        this.ittfPanels.push(retval);
        return retval;
    }
    geop.prototype.getIttfPanel = function(name) {
        var found = null;
        this.ittfPanels.forEach(function(item) {
            found = found || (item.wzName === name ? item : null);
        })
        return found;
    }
    geop.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    geop.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    geop.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
        var items, item;
        items = [];
        this.ittfPanels.forEach(item => 
        
            items.push(item.toJson())
        )
        result['ittfPanels'] = items;
    }
    geop.prototype.loadChild = function(child) {
        var ok = false, name = child.n.toLowerCase();
        if (name === 'ittfpanel') {
            return this.wzLoadToChildColl(child, _md.ittfPanel, this.ittfPanels);
        }
        if (name === 'ittf-panel') {
            return this.wzLoadToChildColl(child, _md.ittfPanel, this.ittfPanels);
        }
        ok = _md.item.prototype.loadChild.call(this, child);
        return ok;
    }
    geop.prototype.loadFromNode = function(node) {
        node.children.forEach((item) => {
        
            var loaded = this.loadChild(item);
            if (!loaded) {
                throw new _md.geopModelException("Tag not recognized: " + item.n, item, this);
            }
        }
        )
    }
    geop.prototype.wzVerify = function(ctx) {
        this.ittfPanels.forEach(item => 
        
            item.wzVerify(ctx)
        )
        _md.item.prototype.wzVerify.call(this, ctx);
    }
    geop.prototype.wzInitialize = function(ctx) {
        this.ittfPanels.forEach(item => 
        
            item.wzInitialize(ctx)
        )
        _md.item.prototype.wzInitialize.call(this, ctx);
    }
    return geop;
})(item);

_md.geop = geop;
// element ittfPanel
var ittfPanel = (function (geopBase) {
    _inherits(ittfPanel, geopBase);
    function ittfPanel(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(ittfPanel.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, ittfPanel);
        this.wzElement = "ittfPanel";
    }
    ittfPanel.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    ittfPanel.prototype.toJsonProperties = function(result) {
        _md.geopBase.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    ittfPanel.prototype.toJsonChildren = function(result) {
        _md.geopBase.prototype.toJsonChildren.call(this, result);
    }
    return ittfPanel;
})(geopBase);

_md.ittfPanel = ittfPanel;
// element fullname
var fullname = (function (item) {
    _inherits(fullname, item);
    function fullname(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(fullname.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, fullname);
        this.wzElement = "fullname";
    }
    fullname.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    fullname.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    fullname.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return fullname;
})(item);

_md.fullname = fullname;
// element title
var title = (function (item) {
    _inherits(title, item);
    function title(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(title.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, title);
        this.wzElement = "title";
    }
    title.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    title.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    title.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return title;
})(item);

_md.title = title;
// element description
var description = (function (item) {
    _inherits(description, item);
    function description(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(description.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, description);
        this.wzElement = "description";
    }
    description.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    description.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    description.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return description;
})(item);

_md.description = description;
// element alias
var alias = (function (item) {
    _inherits(alias, item);
    function alias(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(alias.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, alias);
        this.wzElement = "alias";
    }
    alias.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    alias.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    alias.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return alias;
})(item);

_md.alias = alias;
// element synonim
var synonim = (function (item) {
    _inherits(synonim, item);
    function synonim(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(synonim.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, synonim);
        this.wzElement = "synonim";
    }
    synonim.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    synonim.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    synonim.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return synonim;
})(item);

_md.synonim = synonim;
// element date
var date = (function (item) {
    _inherits(date, item);
    function date(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(date.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, date);
        this.wzElement = "date";
    }
    date.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    date.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    date.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return date;
})(item);

_md.date = date;
// element year
var year = (function (item) {
    _inherits(year, item);
    function year(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(year.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, year);
        this.wzElement = "year";
    }
    year.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    year.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    year.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return year;
})(item);

_md.year = year;
// element period
var period = (function (item) {
    _inherits(period, item);
    function period(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(period.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, period);
        this.wzElement = "period";
    }
    period.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    period.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    period.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return period;
})(item);

_md.period = period;
// element start
var start = (function (item) {
    _inherits(start, item);
    function start(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(start.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, start);
        this.wzElement = "start";
    }
    start.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    start.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    start.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return start;
})(item);

_md.start = start;
// element end
var end = (function (item) {
    _inherits(end, item);
    function end(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(end.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, end);
        this.wzElement = "end";
    }
    end.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    end.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    end.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return end;
})(item);

_md.end = end;
// element place
var place = (function (item) {
    _inherits(place, item);
    function place(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(place.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, place);
        this.wzElement = "place";
    }
    place.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    place.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    place.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return place;
})(item);

_md.place = place;
// element source
var source = (function (item) {
    _inherits(source, item);
    function source(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(source.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, source);
        this.wzElement = "source";
    }
    source.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    source.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    source.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return source;
})(item);

_md.source = source;
// element url
var url = (function (item) {
    _inherits(url, item);
    function url(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(url.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, url);
        this.wzElement = "url";
    }
    url.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    url.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    url.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return url;
})(item);

_md.url = url;
// element ref
var ref = (function (item) {
    _inherits(ref, item);
    function ref(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(ref.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, ref);
        this.wzElement = "ref";
    }
    ref.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    ref.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    ref.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return ref;
})(item);

_md.ref = ref;
// element from
var from = (function (item) {
    _inherits(from, item);
    function from(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(from.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, from);
        this.wzElement = "from";
    }
    from.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    from.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    from.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return from;
})(item);

_md.from = from;
// element to
var to = (function (item) {
    _inherits(to, item);
    function to(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(to.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, to);
        this.wzElement = "to";
    }
    to.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    to.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    to.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return to;
})(item);

_md.to = to;
// element wiki
var wiki = (function (item) {
    _inherits(wiki, item);
    function wiki(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(wiki.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, wiki);
        this.wzElement = "wiki";
    }
    wiki.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    wiki.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    wiki.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return wiki;
})(item);

_md.wiki = wiki;
// element en
var en = (function (item) {
    _inherits(en, item);
    function en(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(en.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, en);
        this.wzElement = "en";
    }
    en.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    en.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    en.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return en;
})(item);

_md.en = en;
// element it
var it = (function (item) {
    _inherits(it, item);
    function it(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(it.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, it);
        this.wzElement = "it";
    }
    it.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    it.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    it.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return it;
})(item);

_md.it = it;
// element context
var context = (function (item) {
    _inherits(context, item);
    function context(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(context.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, context);
        this.wzElement = "context";
    }
    context.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    context.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    context.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return context;
})(item);

_md.context = context;
// element namespace
var namespace = (function (item) {
    _inherits(namespace, item);
    function namespace(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(namespace.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, namespace);
        this.wzElement = "namespace";
    }
    namespace.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    namespace.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    namespace.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return namespace;
})(item);

_md.namespace = namespace;
// element state
var state = (function (item) {
    _inherits(state, item);
    function state(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(state.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, state);
        this.wzElement = "state";
    }
    state.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    state.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    state.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return state;
})(item);

_md.state = state;
// element text
var text = (function (item) {
    _inherits(text, item);
    function text(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(text.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, text);
        this.wzElement = "text";
    }
    text.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    text.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    text.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return text;
})(item);

_md.text = text;
// element li
var li = (function (item) {
    _inherits(li, item);
    function li(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(li.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, li);
        this.wzElement = "li";
    }
    li.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    li.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    li.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return li;
})(item);

_md.li = li;
// element a
var a = (function (item) {
    _inherits(a, item);
    function a(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(a.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, a);
        this.wzElement = "a";
    }
    a.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    a.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
        if (!isEmpty(this.href)) {
            result["href"] = this.href;
        }
        if (!isEmpty(this.title)) {
            result["title"] = this.title;
        }
        if (!isEmpty(this.target)) {
            result["target"] = this.target;
        }
    }
    a.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    a.prototype.loadChild = function(child) {
        var ok = false, name = child.n.toLowerCase();
        if (name === 'href') {
            this.href = child.v; return true;
        }
        if (name === 'title') {
            this.title = child.v; return true;
        }
        if (name === 'target') {
            this.target = child.v; return true;
        }
        ok = _md.item.prototype.loadChild.call(this, child);
        return ok;
    }
    a.prototype.loadFromNode = function(node) {
        node.children.forEach((item) => {
        
            var loaded = this.loadChild(item);
            if (!loaded) {
                throw new _md.geopModelException("Tag not recognized: " + item.n, item, this);
            }
        }
        )
    }
    return a;
})(item);

_md.a = a;
// element img
var img = (function (item) {
    _inherits(img, item);
    function img(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(img.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, img);
        this.wzElement = "img";
    }
    img.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    img.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    img.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return img;
})(item);

_md.img = img;
// element quote
var quote = (function (item) {
    _inherits(quote, item);
    function quote(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(quote.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, quote);
        this.wzElement = "quote";
    }
    quote.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    quote.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    quote.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return quote;
})(item);

_md.quote = quote;
// element author
var author = (function (item) {
    _inherits(author, item);
    function author(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(author.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, author);
        this.wzElement = "author";
    }
    author.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    author.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    author.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return author;
})(item);

_md.author = author;
// element authorsgroup
var authorsgroup = (function (item) {
    _inherits(authorsgroup, item);
    function authorsgroup(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(authorsgroup.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, authorsgroup);
        this.wzElement = "authorsgroup";
    }
    authorsgroup.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    authorsgroup.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    authorsgroup.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return authorsgroup;
})(item);

_md.authorsgroup = authorsgroup;
// element coauthor
var coauthor = (function (item) {
    _inherits(coauthor, item);
    function coauthor(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(coauthor.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, coauthor);
        this.wzElement = "coauthor";
    }
    coauthor.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    coauthor.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    coauthor.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return coauthor;
})(item);

_md.coauthor = coauthor;
// element founder
var founder = (function (item) {
    _inherits(founder, item);
    function founder(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(founder.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, founder);
        this.wzElement = "founder";
    }
    founder.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    founder.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    founder.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return founder;
})(item);

_md.founder = founder;
// element parent
var parent = (function (item) {
    _inherits(parent, item);
    function parent(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(parent.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, parent);
        this.wzElement = "parent";
    }
    parent.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    parent.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    parent.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return parent;
})(item);

_md.parent = parent;
// element child
var child = (function (item) {
    _inherits(child, item);
    function child(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(child.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, child);
        this.wzElement = "child";
    }
    child.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    child.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    child.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return child;
})(item);

_md.child = child;
// element place
var place = (function (item) {
    _inherits(place, item);
    function place(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(place.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, place);
        this.wzElement = "place";
    }
    place.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    place.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    place.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return place;
})(item);

_md.place = place;
// element born
var born = (function (item) {
    _inherits(born, item);
    function born(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(born.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, born);
        this.wzElement = "born";
    }
    born.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    born.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    born.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return born;
})(item);

_md.born = born;
// element died
var died = (function (item) {
    _inherits(died, item);
    function died(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(died.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, died);
        this.wzElement = "died";
    }
    died.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    died.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    died.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return died;
})(item);

_md.died = died;
// element avatar
var avatar = (function (item) {
    _inherits(avatar, item);
    function avatar(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(avatar.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, avatar);
        this.wzElement = "avatar";
    }
    avatar.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    avatar.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    avatar.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return avatar;
})(item);

_md.avatar = avatar;
// element curriculum
var curriculum = (function (item) {
    _inherits(curriculum, item);
    function curriculum(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(curriculum.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, curriculum);
        this.wzElement = "curriculum";
    }
    curriculum.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    curriculum.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    curriculum.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return curriculum;
})(item);

_md.curriculum = curriculum;
// element influenced
var influenced = (function (item) {
    _inherits(influenced, item);
    function influenced(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(influenced.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, influenced);
        this.wzElement = "influenced";
    }
    influenced.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    influenced.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    influenced.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return influenced;
})(item);

_md.influenced = influenced;
// element influencedby
var influencedby = (function (item) {
    _inherits(influencedby, item);
    function influencedby(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(influencedby.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, influencedby);
        this.wzElement = "influencedby";
    }
    influencedby.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    influencedby.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    influencedby.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return influencedby;
})(item);

_md.influencedby = influencedby;
// element attributedby
var attributedby = (function (item) {
    _inherits(attributedby, item);
    function attributedby(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(attributedby.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, attributedby);
        this.wzElement = "attributedby";
    }
    attributedby.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    attributedby.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    attributedby.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return attributedby;
})(item);

_md.attributedby = attributedby;
// element opinionof
var opinionof = (function (item) {
    _inherits(opinionof, item);
    function opinionof(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(opinionof.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, opinionof);
        this.wzElement = "opinionof";
    }
    opinionof.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    opinionof.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    opinionof.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return opinionof;
})(item);

_md.opinionof = opinionof;
// element criticism
var criticism = (function (item) {
    _inherits(criticism, item);
    function criticism(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(criticism.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, criticism);
        this.wzElement = "criticism";
    }
    criticism.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    criticism.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    criticism.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return criticism;
})(item);

_md.criticism = criticism;
// element event
var event = (function (item) {
    _inherits(event, item);
    function event(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(event.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, event);
        this.wzElement = "event";
    }
    event.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    event.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    event.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return event;
})(item);

_md.event = event;
// element war
var war = (function (item) {
    _inherits(war, item);
    function war(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(war.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, war);
        this.wzElement = "war";
    }
    war.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    war.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    war.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return war;
})(item);

_md.war = war;
// element person
var person = (function (item) {
    _inherits(person, item);
    function person(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(person.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, person);
        this.wzElement = "person";
    }
    person.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    person.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    person.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return person;
})(item);

_md.person = person;
// element king
var king = (function (item) {
    _inherits(king, item);
    function king(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(king.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, king);
        this.wzElement = "king";
    }
    king.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    king.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    king.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return king;
})(item);

_md.king = king;
// element minister
var minister = (function (item) {
    _inherits(minister, item);
    function minister(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(minister.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, minister);
        this.wzElement = "minister";
    }
    minister.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    minister.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    minister.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return minister;
})(item);

_md.minister = minister;
// element treaty
var treaty = (function (item) {
    _inherits(treaty, item);
    function treaty(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(treaty.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, treaty);
        this.wzElement = "treaty";
    }
    treaty.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    treaty.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    treaty.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return treaty;
})(item);

_md.treaty = treaty;
// element alliance
var alliance = (function (item) {
    _inherits(alliance, item);
    function alliance(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(alliance.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, alliance);
        this.wzElement = "alliance";
    }
    alliance.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    alliance.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    alliance.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return alliance;
})(item);

_md.alliance = alliance;
// element signature
var signature = (function (item) {
    _inherits(signature, item);
    function signature(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(signature.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, signature);
        this.wzElement = "signature";
    }
    signature.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    signature.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    signature.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return signature;
})(item);

_md.signature = signature;
// element mediator
var mediator = (function (item) {
    _inherits(mediator, item);
    function mediator(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(mediator.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, mediator);
        this.wzElement = "mediator";
    }
    mediator.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    mediator.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    mediator.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return mediator;
})(item);

_md.mediator = mediator;
// element party
var party = (function (item) {
    _inherits(party, item);
    function party(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(party.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, party);
        this.wzElement = "party";
    }
    party.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    party.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    party.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return party;
})(item);

_md.party = party;
// element friends
var friends = (function (item) {
    _inherits(friends, item);
    function friends(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(friends.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, friends);
        this.wzElement = "friends";
    }
    friends.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    friends.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    friends.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return friends;
})(item);

_md.friends = friends;
// element enemies
var enemies = (function (item) {
    _inherits(enemies, item);
    function enemies(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(enemies.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, enemies);
        this.wzElement = "enemies";
    }
    enemies.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    enemies.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    enemies.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return enemies;
})(item);

_md.enemies = enemies;
// element state
var state = (function (item) {
    _inherits(state, item);
    function state(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(state.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, state);
        this.wzElement = "state";
    }
    state.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    state.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    state.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return state;
})(item);

_md.state = state;
// element act
var act = (function (item) {
    _inherits(act, item);
    function act(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(act.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, act);
        this.wzElement = "act";
    }
    act.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    act.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    act.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return act;
})(item);

_md.act = act;
// element repealed
var repealed = (function (item) {
    _inherits(repealed, item);
    function repealed(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(repealed.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, repealed);
        this.wzElement = "repealed";
    }
    repealed.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    repealed.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    repealed.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return repealed;
})(item);

_md.repealed = repealed;
// element status
var status = (function (item) {
    _inherits(status, item);
    function status(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(status.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, status);
        this.wzElement = "status";
    }
    status.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    status.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    status.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return status;
})(item);

_md.status = status;
// element jusSystem
var jusSystem = (function (item) {
    _inherits(jusSystem, item);
    function jusSystem(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(jusSystem.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, jusSystem);
        this.wzElement = "jusSystem";
    }
    jusSystem.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    jusSystem.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    jusSystem.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return jusSystem;
})(item);

_md.jusSystem = jusSystem;
// element jusActType
var jusActType = (function (item) {
    _inherits(jusActType, item);
    function jusActType(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(jusActType.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, jusActType);
        this.wzElement = "jusActType";
    }
    jusActType.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    jusActType.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    jusActType.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return jusActType;
})(item);

_md.jusActType = jusActType;
// element agent
var agent = (function (item) {
    _inherits(agent, item);
    function agent(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(agent.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, agent);
        this.wzElement = "agent";
    }
    agent.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    agent.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    agent.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return agent;
})(item);

_md.agent = agent;
// element king
var king = (function (item) {
    _inherits(king, item);
    function king(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(king.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, king);
        this.wzElement = "king";
    }
    king.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    king.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    king.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return king;
})(item);

_md.king = king;
// element pretender
var pretender = (function (item) {
    _inherits(pretender, item);
    function pretender(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(pretender.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, pretender);
        this.wzElement = "pretender";
    }
    pretender.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    pretender.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    pretender.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return pretender;
})(item);

_md.pretender = pretender;
// element house
var house = (function (item) {
    _inherits(house, item);
    function house(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(house.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, house);
        this.wzElement = "house";
    }
    house.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    house.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    house.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return house;
})(item);

_md.house = house;
// element inCharge
var inCharge = (function (item) {
    _inherits(inCharge, item);
    function inCharge(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(inCharge.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, inCharge);
        this.wzElement = "inCharge";
    }
    inCharge.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    inCharge.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    inCharge.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return inCharge;
})(item);

_md.inCharge = inCharge;
// element regent
var regent = (function (item) {
    _inherits(regent, item);
    function regent(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(regent.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, regent);
        this.wzElement = "regent";
    }
    regent.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    regent.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    regent.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return regent;
})(item);

_md.regent = regent;
// element minister
var minister = (function (item) {
    _inherits(minister, item);
    function minister(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(minister.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, minister);
        this.wzElement = "minister";
    }
    minister.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    minister.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    minister.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return minister;
})(item);

_md.minister = minister;
// element primeMinister
var primeMinister = (function (item) {
    _inherits(primeMinister, item);
    function primeMinister(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(primeMinister.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, primeMinister);
        this.wzElement = "primeMinister";
    }
    primeMinister.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    primeMinister.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    primeMinister.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return primeMinister;
})(item);

_md.primeMinister = primeMinister;
// element foreignSecretary
var foreignSecretary = (function (item) {
    _inherits(foreignSecretary, item);
    function foreignSecretary(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(foreignSecretary.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, foreignSecretary);
        this.wzElement = "foreignSecretary";
    }
    foreignSecretary.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    foreignSecretary.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    foreignSecretary.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return foreignSecretary;
})(item);

_md.foreignSecretary = foreignSecretary;
// element homeSecretary
var homeSecretary = (function (item) {
    _inherits(homeSecretary, item);
    function homeSecretary(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(homeSecretary.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, homeSecretary);
        this.wzElement = "homeSecretary";
    }
    homeSecretary.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    homeSecretary.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    homeSecretary.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return homeSecretary;
})(item);

_md.homeSecretary = homeSecretary;
// element exchequer
var exchequer = (function (item) {
    _inherits(exchequer, item);
    function exchequer(name, sourceLineInfo) {
        _get(Object.getPrototypeOf(exchequer.prototype), 'constructor', this).call(this, name,sourceLineInfo);
        _classCallCheck(this, exchequer);
        this.wzElement = "exchequer";
    }
    exchequer.prototype.toJson = function() {
        var result = {};
        this.toJsonProperties(result);
        this.toJsonChildren(result);
        return result;
    }
    exchequer.prototype.toJsonProperties = function(result) {
        _md.item.prototype.toJsonProperties.call(this, result);
        if (this.__jsonProps) {
            for (var k in this.__jsonProps) {
                result[k] = this.__jsonProps[k];
            }
        }
    }
    exchequer.prototype.toJsonChildren = function(result) {
        _md.item.prototype.toJsonChildren.call(this, result);
    }
    return exchequer;
})(item);

_md.exchequer = exchequer;
_md.__tagElementMapping = { '+': 'text', 'authors-group': 'authorsgroup', 'co-author': 'coauthor', 'influenced-by': 'influencedby', 'attributed-by': 'attributedby', 'opinion-of': 'opinionof', 'jus-system': 'jusSystem', 'jussystem': 'jusSystem', 'jus-act-type': 'jusActType', 'jusacttype': 'jusActType', 'in-charge': 'inCharge', 'incharge': 'inCharge', 'prime-minister': 'primeMinister', 'primeminister': 'primeMinister', 'foreign-secretary': 'foreignSecretary', 'foreignsecretary': 'foreignSecretary', 'home-secretary': 'homeSecretary', 'homesecretary': 'homeSecretary', 'ittf-panel': 'ittfPanel', 'ittfpanel': 'ittfPanel' };
// model/replaceUnknownElement( )
var geopModelException = (function () {
    function geopModelException(message, node, instance) {
        _classCallCheck(this, geopModelException);
        // VIA 14/2/21 (pollutes log) set this.node = node
        // VIA 14/2/21 (pollutes log) set this.instance = instance
        if (node && instance) {
            this.message = message + ', wzElement: ' + node.wzElement + ', wzName:' + (node.wzName || '') + ', row:' + node.r + ', col:' + node.c + ', source:' + instance.wzSourceFilepath(node.u);
            this.errorLines = instance.wzSourceErrorLines(node, message)
            ;
        }
        else if (instance) {
            this.message = message + instance.wzSourceLineInfo.toString(instance);
        }
        else {
            this.message = message;
        }
        console.log("[31m%s[0m", 'message', this.message);
        this.stack = (new Error()).stack;
    }
    geopModelException.prototype.toString = function() {
        var msg = [];
        msg.push(chalk.red('Error: ' + this.message));
        msg.push(chalk.red('  name: geopModelException'));
        if (this.node) {
            msg.push(chalk.yellow('  row: ' + this.node.r + ', col: ' + this.node.c));
        }
        if (this.instance) {
            if (this.node) {
                msg.push(chalk.yellow('  uri: ' + this.instance.wzSourceFilepath(this.node.u)));
            }
            else {
                msg.push(chalk.yellow('  uri: ' + this.instance.wzSourceLineInfo.toString(this.instance)));
            }
        }
        else {
            msg.push(chalk.yellow('  uri: unknown'));
        }
        if (this.errorLines) {
            var i, i_items=this.errorLines, i_len=this.errorLines.length, line;
            for (i=0; i<i_len; i++) {
                line = this.errorLines[i];
                msg.push(chalk.yellow('  ' + line));
            }
        }
        return msg.join('\n');
    }
    return geopModelException;
})();

_md.geopModelException = geopModelException;
var geopContext = (function () {
    function geopContext() {
        _classCallCheck(this, geopContext);
        this.validationErrors = [];
    }
    geopContext.prototype.schemaIsValid = function() {
        return this.validationErrors.length == 0;
    }
    geopContext.prototype.addError = function(message, node) {
        var at = node ? ' At ' + node.wzSourceLineInfo.toString(node) : '';
        this.validationErrors.push(message + at)
    }
    return geopContext;
})();

_md.geopContext = geopContext;
function parsestring(value, defaultValue, node) {
    if (isEmpty( value )) {
        return defaultValue;
    }
    return value;
}
function isString(value) {
    return (typeof value === 'string' || value instanceof String);
}
function isEmpty(value) {
    return !isString( value ) || value.length === 0;
}
function isBoolean(value) {
    return value === 'true' || value === 'false';
}

