$group
    
    slide()
        section( Conclusioni

    slide( L'importanza delle Cure Primarie, Sintesi
        div( left
            [ paras
                "Le cure primarie sono lo standard di cura preferito per i bambini con patologie croniche${dot}"
                "Vi è una correlazione diretta fra perdita di peso e benefici per la salute${dot}"
                "Il corso naturale dell'obesità prevede risposte al trattamento e successive ricadute${dot}"

        image( right, Trovare immagine.jpg

    slide( Le Difficoltà del Pediatra, Barriere
        list( top-left, Il dilemma obesità tra:
            [ items
                "Gravità e ingravescenza"
                "e la difficoltà ad affrontarla in modo efficace${dot}"

        list( bottom-left, Le Barriere:
            [ items
                "Tempo e risorse${dot}"
                "Conoscenza${dot}"
                "Consapevolezza${dot}"
                "Autoefficacia${dot}"
                "Aspettativa di risultato${dot}"
                "La sottostima del problema da parte dei genitori${dot}"

        image( right, Trovare immagine.jpg

    slide( La formazione necessaria per i Pediatri, Strumenti
        list( left, Secondo l’AAP deve includere:
            [ items
                "La base fisiologica sottostante per la disregolazione del peso${dot}"
                "L'Intervento Motivazionale${dot}"
                "La distorsione del peso${dot}"
                "L'impatto sociale ed emotivo dell'obesità sui pazienti${dot}"
                "La necessità di adattare la gestione ai determinanti sociali della salute che influiscono sul peso e sui risultati relativi al peso${dot}"

        image( right, Trovare immagine.jpg

    slide( La rete dei servizi, Strumenti

        image( full, Cosie project.png
            margin 15

    slide( Per le famiglie, Strumenti


        $*
        Le guide anticipatorie - immagini e app Beba in unica diapo
        diapo guide anticipatorie
        simbolo beba
        immagine guide
        La app beba -immagini
        mostrare tutti a tavola, la mappa delle opportunità, la dieta del giorno).
        *$
        image-rect( Trovare immagine.jpg, 20% 20% 30% 40%

    slide( La ricerca nelle Cure Primarie, Strumenti

        text-rect( La ricerca sui determinanti alla AUSL RE, 30% 20% 10% 40%

        text-rect( dummy, 45% 20% 10% 40%
            text "PRENATAL, BIRTH, AND POSTNATAL MEDIATORS OF MATERNAL EDUCATION LEVEL AND CHILDHOOD OBESITY AT FIVE YEARS"

        text-rect( dummy, 55% 20% 10% 40%
            text "A cura di Cure Primarie, Epidemiologia"

        text-rect( Il Trial 2013 su Pediatrics, 20% 70% 10% 35%
        
        image-rect( trial clinico pediatrics.jpg, 30% 70% 20% 20%
        

        image-rect( Gruppo cocreation.jpg, 70% 70% 25% 25%

    slide( Culture for health, Strumenti        

        div( top-left
            [ paras
                "La partecipazione attiva e continua ad esperienze culturali ed artistiche promuove salute e previene le malattie${dot}"
                "Dobbiamo rendere accessibili queste esperienze alle famiglie più svantaggiate${dot}"

        list( bottom-left, Ipotesi cross over culturali applicati all’Obesità nei ragazzi:
            [ items
                "Una scuola di break-dance e di canto rap  per i ragazzi  con obesità${dot}"
                "Un device (per smartphone o indossabile) per incentivare i giovani al movimento e all’impegno in comportamenti sani${dot}"
                "La narrazione: lavorare su gruppi di auto aiuto${dot}"

        image( right, rapper climatico.jpg, Una scuola di rap 

    slide( Per i Pediatri, Sfide

        list( left, Le principali sfide sono:
            [ items
                "Non stigmatizzare${dot}"
                "Sfruttare il potenziale della continuità delle cure${dot}"
                "Offrire messaggi chiave per le varie età, specifici secondo le indicazioni dei criteri di salute${dot}"
                "Offrire al genitore gli elementi per formare un’alleanza terapeutica (curve di crescita, messaggi sulla salute, informazioni per le varie età)${dot}"
                "Porre attenzione alla sensibilità, anche culturale, del paziente${dot}"

    slide( Per i Pediatri, Sfide

        div( top-left
            [ paras
                "E' importante tenere presente che i pazienti e le famiglie hanno a cuore la loro salute e quella dei loro figli e agli operatori sanitari deve essere ricordato che la presenza di sovrappeso o obesità NON è un'indicazione di cattiva genitorialità"
        
        div( bottom-left
            [ paras
                "Ove la diagnosi ne rilevi la necessità intervenire terapeuticamente"

