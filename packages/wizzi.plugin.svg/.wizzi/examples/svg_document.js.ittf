module
    kind es6
    $
        var md = {
            name: 'svg',
            artifact: 'document',
        };
    v08/examples/plugin( &md )
        $append plugins
            [ items
                @ './index.js'
                @ '@wizzi/plugin.js'
            @ pluginsBaseFolder
                _ path.resolve
                    @ __dirname
                    @ '..'
        let arg = process.argv[2]
        const moduleName = arg && arg.length > 0 ? arg : 'first'
        _ executeGenerateModules
            [
                @ moduleName
            a_cb_no_throw( result )
                if err
                    error 'svg/document.examples.executeGenerateModules.err', err
                    error 'svg/document.examples.executeGenerateModules.err.toString()', err.toString()
                    if err.inner
                        error 'svg/document.examples.executeGenerateModules.err.inner.toString()', err.inner.toString()
                else
        function executeGenerateModules
            param modules
            param callback
            _ async.mapSeries
                @ modules
                =>
                    param module
                    param callback
                    var ittfDocumentUri
                        _ path.join
                            @ __dirname
                            @ 'ittf'
                            @ module + '.svg.ittf'
                    var outputPath
                        _ path.join
                            @ __dirname
                            @ 'results'
                            @ 'svg'
                            @ module + '-document.g.svg'
                    _ loadModelAndGenerateArtifact
                        @ ittfDocumentUri
                        {
                        @ 'svg/document'
                        a_cb( artifactText )
                            _ file.write
                                @ outputPath
                                @ artifactText
                            r_cb( artifactText )
                @ callback
