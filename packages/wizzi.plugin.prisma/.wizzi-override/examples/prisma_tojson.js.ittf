module
    kind es6
    $
        var md = {
            name: 'prisma',
            artifact: 'tojson',
        };
    wizziplugin/examples/plugin( &md )
        $append plugins
            [ items
                @ './wizzi.plugin.prisma/index.js'
                @ './wizzi.plugin.json/index.js'
            @ pluginsBaseFolder "C:/My/wizzi/stfnbssl/wizzi.plugins/packages"
        let arg = process.argv[2]
        const moduleName = arg && arg.length > 0 ? arg : 'first'
        _ getWzCtx
            _ path.resolve
                @ __dirname
                @ '..'
                @ '.wizzi-override'
                @ 'models'
            a_cb_no_throw( wzCtx )
                if err
                    error err
                    return
                _ executeGenerateModules
                    [
                        @ moduleName
                    @ wzCtx
                    a_cb_no_throw( result )
                        if err
                            error 'prisma/document.examples.executeGenerateModules.err', err
                            error 'prisma/document.examples.executeGenerateModules.err.toString()', err.toString()
                            if err.inner
                                error 'prisma/document.examples.executeGenerateModules.err.inner.toString()', err.inner.toString()
                        else
        function executeGenerateModules
            param modules
            param wzCtx
            param callback
            _ async.mapSeries
                @ modules
                =>
                    param module
                    param callback
                    var ittfDocumentUri
                        _ path.join
                            @ __dirname
                            @ 'ittf'
                            @ module + '.prisma.ittf'
                    var outputPath
                        _ path.join
                            @ __dirname
                            @ 'results'
                            @ 'prisma'
                            @ module + '-document.g.json'
                    _ loadWizziModel
                        @ ittfDocumentUri
                        {
                            @ wzCtx wzCtx
                        a_cb( wizziModel )
                            _ loadModelAndGenerateArtifact
                                @ ittfDocumentUri
                                {
                                    @ wzCtx wzCtx
                                @ 'prisma/tojson'
                                a_cb( artifactText )
                                    _ file.write
                                        @ outputPath
                                        @ artifactText
                                    r_cb( artifactText )
                @ callback
