module
    kind es6
    $
        var md = {
            name: 'pdf',
        };
    v08/examples/plugin(&md)
        $append plugins
            [ items
                @ './index.js'
            @ pluginsBaseFolder
                _ path.resolve
                    @ __dirname
                    @ '..'
        _ executeGenerateModules
            [
                @ 'first'
                @ 'tables'
                @ 'columns'
                @ 'page'
            f_cb_no_throw( result )
                if err
                    log 'pdf.examples.executeGenerateModules.err', err
                    log 'pdf.examples.executeGenerateModules.err.toString()', err.toString()
                    if err.inner
                        log 'pdf.examples.executeGenerateModules.err.inner.toString()', err.inner.toString()
                else
                    log 'pdf.examples.executeGenerateModules.result', result
        function executeGenerateModules
            param modules
            param callback
            _ async.mapSeries
                @ modules
                =>
                    param module
                    param callback
                    var ittfDocumentUri
                        _ path.join
                            @ __dirname
                            @ 'ittf'
                            @ module + '.pdf.ittf'
                    var outputPath
                        _ path.join
                            @ __dirname
                            @ 'dist'
                            @ module + '.pdf.js'
                    _ loadModelAndGenerateArtifact
                        @ ittfDocumentUri
                        {
                        @ 'pdf/document'
                        f_cb( artifactText )
                            _ file.write
                                @ outputPath
                                @ artifactText
                            r_cb( artifactText )
                @ callback
